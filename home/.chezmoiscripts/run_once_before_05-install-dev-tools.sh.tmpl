{{ if eq .chezmoi.os "linux" -}}
#!/bin/bash
# ==============================================================================
# 05-INSTALL-DEV-TOOLS
# Development tools: mise-managed languages (Node, Bun, Python/uv)
# ==============================================================================

set -euo pipefail

# Colors
BLUE='\033[0;34m'
GREEN='\033[0;32m'
NC='\033[0m'

echo -e "${BLUE}━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━${NC}"
echo -e "${BLUE}  💻 Setting Up Development Tools${NC}"
echo -e "${BLUE}━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━${NC}"

# Ensure mise is installed
if ! command -v mise &>/dev/null; then
    curl -s https://mise.run 2>/dev/null | sh 2>/dev/null
fi

# Activate mise
eval "$(mise activate bash)" 2>/dev/null || true

# Install configured tools
if [ -f "$HOME/.config/mise/config.toml" ]; then
    mise install -y &>/dev/null || true
fi

# Install global tools
mise use -g bun@latest &>/dev/null 2>&1 || true
mise use -g node@lts &>/dev/null 2>&1 || true
mise use -g uv@latest &>/dev/null 2>&1 || true

echo -e "${GREEN}✓ Development tools configured${NC}"
{{ end -}}
